{
  "name": "belote-offline-qa",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:rules": "playwright test tests/rules-agent.spec.mjs --project=chromium",
    "test:design": "playwright test tests/design-agent.spec.mjs --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
